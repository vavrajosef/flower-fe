apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flower-fe.fullname" . }}-test-connection"
  labels:
    {{- include "flower-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flower-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
