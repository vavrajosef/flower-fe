apiVersion: v1
kind: Service
metadata:
  name: {{ include "flower-fe.fullname" . }}
  labels:
    {{- include "flower-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flower-fe.selectorLabels" . | nindent 4 }}
